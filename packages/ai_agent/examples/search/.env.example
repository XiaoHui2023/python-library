LLM_API_KEY=

LLM_BASE_URL=https://api.openai.com/v1

LLM_MODEL=gpt-4o-mini

LLM_THINKING_ENABLED=true



# MCP：复制 mcp.json.example → mcp.json（gitignore），填写 CURSOR_API_KEY 与 tools 绝对路径。
# 本地开发可把各 tools/<name>/mcp.json 的 mcpServers 段合并进本目录 mcp.json（uvx --from，与 tools 一致）。
# 验收：examples/search/run_demo.bat 或包根 run.bat examples/search（线性执行检查→探测→演示）

